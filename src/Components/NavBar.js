import React from "react";
import AppBar from "material-ui/AppBar";

const NavBar = () => {
  return (
    <div className="navbar">
      <AppBar title="Lists github repository" />
    </div>
  );
};

export default NavBar;
